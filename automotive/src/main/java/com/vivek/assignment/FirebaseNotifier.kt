package com.vivek.assignment

class FirebaseNotifier {
    fun sendAlert(customer: Customer, message: String) {
        // TODO: Replace with real Firebase Cloud Messaging (FCM) logic
        println("Sending Firebase alert for customer ${customer.name}: $message")
    }
}